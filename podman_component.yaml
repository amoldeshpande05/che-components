apiVersion: v1
kind: Pod
metadata:
 name: podman
spec:
 containers:
   - name: container
     image: amoldeshpande/custom-podman-root
     args:
       - sleep
       - "infinity"
     securityContext:
       privileged: true